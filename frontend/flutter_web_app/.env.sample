# Firebase Authentication設定
FIREBASE_API_KEY=your-api-key           # Firebase Console > Project Settings > Web App > Config の apiKey
FIREBASE_AUTH_DOMAIN=your-auth-domain   # Firebase Console > Project Settings > Web App > Config の authDomain
FIREBASE_PROJECT_ID=your-project-id     # Firebase Console > Project Settings > Web App > Config の projectId
FIREBASE_APP_ID=your-app-id            # Firebase Console > Project Settings > Web App > Config の appId
FIREBASE_MESSAGING_SENDER_ID=your-id    # Firebase Console > Project Settings > Web App > Config の messagingSenderId

# バックエンドAPI設定
API_BASE_URL=http://localhost:8080      # ローカル開発用のデフォルト値
                                       # Cloud Run環境の場合は実際のエンドポイントに変更してください

# 注意：このファイルは.envファイルのテンプレートです。
# 実際の値は.envファイルに設定してください。
# .envファイルはGitにコミットしないでください。